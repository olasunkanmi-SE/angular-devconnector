export interface AuthPayload {
  email: string;
  passord: string;
}

export interface registerPayload {
  name: string;
  email: string;
  password: string;
  confirmPassword: string;
  gender: string;
}
